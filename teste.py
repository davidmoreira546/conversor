import os #conversao basica de arquivos de video e audio. ex:mp3,mp4,ts,mov
import aspose.words as aw #conversao de imagem e pdfs

def converter_video(arquivo,dir):
    option = os.system('ls')
    for i in range(option):
        print(f'{i}')
    print('insira diretorio')
    dir = input(' ')
    arquivo = len
